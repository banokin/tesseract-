# Optional runtime backends for /scan-passport-russian-docs-ocr.
#
# Install this before requirements-russian-docs-ocr.txt:
#
#   pip install -r requirements-russian-docs-ocr-runtime.txt
#   pip install --no-deps -r requirements-russian-docs-ocr.txt
#
# ONNX is the recommended default for local CPU experiments.
onnxruntime==1.18.1

# Needed only if you select OpenVINO on the frontend page.
openvino==2026.1.0

# Needed only if you select CoreML on the frontend page (macOS only).
coremltools==9.0
